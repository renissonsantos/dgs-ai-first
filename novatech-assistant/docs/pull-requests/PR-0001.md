# PR-0001 — Estruturação do projeto: AGENTS.md, arquitetura MCP e skills técnicas (Tech Lead, Cenário 2)

> Branch (local): `feat/cenario2-tech-lead` · Revisão simulada localmente.

## Objetivo
Entregar os artefatos do Tech Lead da fase de estruturação: a constitution do projeto
(`AGENTS.md`), a arquitetura de MCP com health check executável, e a primeira skill técnica
testada empiricamente — todos validados gerando código real que compila e passa nos testes.

## Mudanças

### Ex. 2.1 — AGENTS.md (construído e testado)
- `AGENTS.md` (v2) — Project Overview, Tech Stack & Architecture (com orçamento de contexto da
  ADR-0002), Coding Standards (DEVE/NÃO DEVE), Build & Deploy + validation gates.
- `docs/agents-md/AGENTS-v1.md`, `AGENTS-v2.md`, `teste-agente.md`, `round1/` — ciclo de teste
  com agente e iteração v1→v2.
- Código gerado na rodada 2 (segue o v2): `src/shared/*`, `src/services/*`,
  `src/functions/query/*`, `tests/integration/query.test.ts`.

### Ex. 2.2 — Arquitetura de MCP
- `.mcp/mcp.json` — 4 servers locais (least privilege; filesystem rw + filesystem-docs RO).
- `scripts/mcp-health-check.mjs` — health check (handshake MCP + tools/list + checagem de escopo).
- `docs/mcp/arquitetura-mcp.md`, `docs/mcp/health-check-output.md` — arquitetura, política de
  aprovação, monitoramento, versionamento, contingência, e **saída de execução real** (4/4 OK).

### Ex. 2.3 — Skill `azure-functions-endpoint`
- `skills/domain/azure-functions-endpoint.md` (v2) — testada e iterada.
- `docs/skills/azure-functions-endpoint-v1.md`, `teste-azure-functions-endpoint.md`, `round1/`.
- Código gerado na rodada 2: `src/services/feedback-store.ts`, `src/functions/feedback/*`,
  `tests/integration/feedback.test.ts`.

## Validação (executada)
- `npx vitest run` → **9 passed** (query 4 + feedback 2 + health 3).
- `npx tsc -p . --noEmit` → **EXIT 0**.
- `npx eslint src tests` → **OK** (regra `no-console` ativa e comprovada).
- `node scripts/mcp-health-check.mjs` → **4/4 servers OK**.
- Read-only das fontes de negócio: escrita → `EPERM`; leitura OK.

## Checklist dos validation gates
- [x] Gate 1 (Spec→Plan): N/A neste PR (sem nova spec; usa requirements/plan simulados).
- [x] Gate 2 (Tasks→Implement): implementação segue os padrões do AGENTS.md/skill.
- [x] Gate 3 (Code→Merge): code review do TL — diff revisável; testes verdes.
- [x] Gate 4 (Tests→Deploy): cobertura de cenários (happy path + validação + baixa confiança).

## Notas
- Decisões técnicas rastreiam as ADRs do cenário 1 (ADR-0001 GPT-4o, ADR-0002 orçamento de
  contexto, ADR-0003 versão vigente).
- Seções de Product Rules / Testing Standards / Project Management ficam para PS / QA / DM.
